#!/usr/bin/env bash

echo done1 $SHELL
# brew uninstall node
# curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.33.5/install.sh | bash

# read -r -d '' NVM_CONTENT << eom1
# "export NVM_DIR=\"$HOME/.nvm\"
# [ -s \"$NVM_DIR/nvm.sh\" ] && . \"$NVM_DIR/nvm.sh\""
# eom1

# [[ $SHELL == "bash" ]] && $(echo $NVM_CONTENT >> "~/.bash_profile" && echo $NVM_CONTENT >> "~/.bashrc")
# [[ $SHELL == "zsh" ]] && $(echo $NVM_CONTENT >> "~/.zshrc")
